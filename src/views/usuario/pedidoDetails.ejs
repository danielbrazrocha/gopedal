        <h2>Lista de Produto(s) no Pedido  <span class="search-error-alert"><%= pedidoDetails[0].OrderDetailId %></span></h2>
        <div class="table-responsive">
          <table class="table table-striped table-sm">
            <thead>
              <tr>
                <th scope="col">#</th>
                <th scope="col">Nome do Produto</th>
                <th scope="col">Preço</th>
                <th scope="col">Quantidade</th>
              </tr>
            </thead>
            <tbody>
                
                <% for (let i = 0; i < pedidoDetails.length; i++) { %>
                    <tr>
                        <td><%= i+1 %></td>
                        <td><%= pedidoDetails[i].product.name %></td>
                        <td><%= pedidoDetails[i].product.price.toLocaleString('pt-BR', { minimumFractionDigits: 2 , style: 'currency', currency: 'BRL' }) %></td>
                        <td><%= pedidoDetails[i].quantity %></td>
                    </tr>
                <% } %>

         
            </tbody>
          </table>
    
    <!-- Button trigger modal -->
    <a href="/usuario/pedidos"        
        class="btn btn-secondary">
        Voltar
    </a>
      
        </div>
    